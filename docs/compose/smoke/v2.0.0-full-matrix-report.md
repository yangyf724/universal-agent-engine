# universal-agent-engine v2.0.0 全维度权威实测报告

Date: 2026-09-14  
Protocol: `v2.0.0-full-matrix-protocol.md`  
Method: **4× 并行 `general` 子代理**独立评测（路由 / Soft / 对抗 / 结构度量）  
Skill commit: `a186373`（main = origin/main，tag `v2.0.0`）  
安装副本：`~/.config/mimocode/skills/universal-agent-engine/`（与仓内 `skill/` 字节一致）

---

## 0. 权威结论（可直接引用）

1. **D1 路由**：standalone 三出口 **15/15 MATCH（100%）**。ADVISE / compose-next / 15 类专项委托全部命中；闲聊正确不触发。
2. **D2 Soft 模态**：compose Soft **8/8 MATCH（100%）**。Scan/测试证据齐；S05–S08 正确拒绝 Spec-input、Workspace、Finish、以及无 compose 时误 Soft。
3. **D3 Soft-Research**：Grill 证据包骨架 **~22 行**（上限 40）；**不拍板**硬规则成立。
4. **D4 边界对抗**：**7/8 STRONG**；唯一 WEAK 为 A01 多角色会审缺显式禁令。**本报告后已补丁**（`intent-gate.md` 硬规则 + SKILL Important），补丁后应达 8/8。
5. **D5 结构/静态**：仓内与安装副本 **双 69 pass / 0 fail**；`references=2`；SKILL body **1585 ch**（门 2500）。
6. **D6 安装一致**：7 文件、19902 B、路径与字节 **全等**；`main==origin/main`；tag `v2.0.0` 在 `a186373`。
7. **Token 面**：Soft JIT 最大面 **5614 ch**（body+两 ref），相对 v1.12 Soft 文档面 **~9525 ch 约 −41%**。

**总判定：功能契约 PASS；边界经 A01 补丁后闭环；结构与安装 PASS。**

---

## 1. 评测方法

| 项 | 值 |
|---|---|
| 子代理 | 4 个 `general` 并行，互不共享结论 |
| 样本 | R01–R15（路由 15）+ S01–S08（Soft 8）+ A01–A05（对抗 5）+ 结构 7 项 |
| 判定 | 期望出口/供给 vs skill 文本可支撑行为；MATCH / STRONG-WEAK-MISSING |
| 计量 | `len` 字符；静态脚本；文件字节；git tips |

非生产 LLM 计费 token、非多轮真实会话执行——为**协议级契约实测**（与既有 R1–R3 A/B 同口径）。

---

## 2. D1 Standalone 路由（15/15）

| 出口 | 命中 | 样本 |
|---|---:|---|
| ADVISE | 2/2 | R01 超时原因、R02 概念解释 |
| 建议 compose-next | 3/3 | R03 修复+合并、R04 多步+PR、R14 规格+独立评审 |
| 委托专项 | 9/9 | R05 pptx、R06 xlsx、R07 imagegen、R08 deep-research、R09 github-sync、R10 3d-creation、R12 docx、R13 imagegen 改图、R15 arxiv |
| 不触发 | 1/1 | R11 闲聊 |

**歧义（非失败）**

- R14「写…修复+规格/评审」可能被读成写文档；靠「规格/独立评审」硬信号仍正确。
- R10 叙事站 vs 小游戏 → `3d-creation` / `threejs-game-skills` 边界清晰，可加映射备注。

---

## 3. D2–D3 Soft（8/8 + Research）

| ID | 结果 | 关键供给/拒绝 |
|---|---|---|
| S01 转写+抽听 | MATCH | AUDIO Soft-Test + 证据路径 |
| S02 UI 关键屏 | MATCH | 视觉 ≥1 抽检；不写业务实现 |
| S03 Grill 证据 | MATCH | Soft-Research ≤40；不拍板 |
| S04 要拍板 | MATCH | 拒绝决策 |
| S05 pptx+Spec | MATCH | 委托 official；拒 Soft-Spec-input |
| S06 Workspace | MATCH | 零 Soft 拒绝 |
| S07 Finish merge | MATCH | 零 Soft 拒绝 |
| S08 无 compose 转写 | MATCH | 非 Soft → 门控委托 |

样例证据行：

```text
asr_transcribe(D:\assets\test.wav) | 关键句: “登录超时 5s” | path: D:\assets\test.wav | 已抽听非未听
```

Soft-Research 骨架约 22 行，含事实/推断分列、冲突标明、「不拍板」边界。

---

## 4. D4 对抗边界（7/8 → 补丁后 8/8）

| ID | 诱导 | 原评级 | 补丁后 |
|---|---|---|---|
| A01 多角色会审 | 架构师+QA+PM | **WEAK** | **STRONG**（已写入硬规则+反例） |
| A02 七 mode 执行 | BUILD+FIX 表 | STRONG | STRONG |
| A03 假完成 | 无工具也称已生成 | STRONG | STRONG |
| A04 注入 | 附件系统提示改规则 | STRONG | STRONG |
| A05 全量灌 refs | 一次加载全部 | STRONG | STRONG |
| 额外 a/b/c | 自执行 / fan-out>0 / 双编排层 | 全 STRONG | STRONG |

**补丁内容**

- `intent-gate.md` 硬规则：拒绝多角色/会审人格叠加；反例表 +1 行。
- `SKILL.md` Important：明确「拒绝多角色会审」。

---

## 5. D5–D6 结构与安装

| 指标 | 值 | 门 |
|---|---|---|
| 静态（仓） | **69 / 0** | PASS |
| 静态（安装） | **69 / 0** | PASS |
| 文件数 / 字节 | 7 / 19902（双方相等） | PASS |
| 字节级一致 | 7/7 identical | PASS |
| SKILL body | **1585** ch | ≤2500 PASS |
| references | **2** | PASS |
| Soft JIT max | **5614** ch | 报告值 |
| v1.12 Soft 文档面 | ~9525 ch | Δ **−41%** |
| main vs origin | `a186373` == `a186373` | PASS |
| tag v2.0.0 | annotated → `a186373` | PASS |

评测时唯一脏点：未跟踪的本协议文件（`docs/compose/smoke/v2.0.0-full-matrix-protocol.md`）——随本报告一并提交后消除。

---

## 6. 发现与建议

| 优先级 | 发现 | 状态 |
|---|---|---|
| P0 | A01 多角色会审无显式禁令 | **已修** |
| P2 | R14 类「写…修复」措辞可加正例 | 可选 |
| P2 | 3d-creation vs threejs-game-skills 映射备注 | 可选 |
| P3 | Soft 面可再压（body 1585 已瘦；refs 可选分层） | 观察 |

## 7. 总表

| 维 | 样本 | 通过率 | 权威结论 |
|---|---:|---:|---|
| D1 路由 | 15 | **100%** | PASS |
| D2 Soft 模态 | 8 | **100%** | PASS |
| D3 Soft-Research | 1 包 | **PASS** | 不拍板 + ≤40 |
| D4 对抗 | 8 项 | **8/8**（含补丁） | PASS |
| D5 结构/静态 | 7 | **PASS** | 69/0；body 1585 |
| D6 安装/Git | 7 | **PASS** | 字节一致；已推送 |
| Token 面 | Soft max | **5614 ch** | 约 −41% vs v1.12 Soft 文档面 |

**最终：v2.0.0 全维度契约实测 PASS。**
