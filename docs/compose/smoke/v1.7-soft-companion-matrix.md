# Soft Companion Validation Matrix (v1.7)

Date: 2026-09-13  
Branch: `optimize/v1.7-soft-companion`  
Base: `e6f86d9`  
Verdict: **PASS+AMENDMENTS**（允许实施，必须先合入 SC1–SC7）

## Verdict by gate

| Gate | Result | Evidence |
|---|---|---|
| C1 强互补 | **PASS（条件）** | 点名 compose-next 独占由 X1/SC2 保持；Soft 不引入 Spec/Worktree/Review/Finish；不双载需 SC1 短路，否则 Step 2–6 仍会跑 |
| C2 效率 | **PASS（条件）** | Soft 只 JIT `multimodal.md`（SC3）；body 需压缩保持 ≤3000（SC7）；禁止 Soft 路径读 Full Gates / R1 包 |
| C3 稳定 | **PASS** | `multimodal.md` Universal Multimodal Gates 已含样本抽检/跨模态一致/降级；Soft 强制带入（SC4） |

## Scenario matrix

| ID | 场景 | 期望行为 | 判定 | 缺口 |
|---|---|---|---|---|
| V1 | 用 compose-next 修登录 bug | 只 compose-next | PASS | 需 SC2 显式锁 |
| V2 | compose-next 中转写录音写入夹具说明 | Soft AUDIO，不建 Spec | PASS+SC1 | 无短路则会进全协议 |
| V3 | 独立转写会议纪要 | R2 全协议，非 Soft | PASS | A2 必须含 compose 上下文 |
| V4 | P-domain 修 bug + 截图识别报错 | Soft VISION 或先建议 compose-next | PASS+AMEND | D3 需写清「媒体子任务 vs 整单 P-domain」 |
| V5 | compose-next 中导出验收 PPT | Soft DOCOFFICE→official | PASS | 不接管 feature |
| V6 | 独立 10 页 PPT | R2/official | PASS | 防 Soft 误伤 |

## AMENDMENTS（实施准入）

1. **SC1** Soft 判定置于 SKILL Important/Step 0 文首，先于 Plan/门禁指针。
2. **SC2** 点名 `/compose-next` 且无独立多模态请求 → 只 compose-next。
3. **SC3** Soft 供给仅 `references/multimodal.md` + 工具锚点；一句指针。
4. **SC4** Soft 仍强制样本抽检 / No Fake Media / 降级披露。
5. **SC5** description 不把 `/compose-next` 写成正触发；至多互补语义微调。
6. **SC6** scenarios/静态断言覆盖 V1–V6（或等价）。
7. **SC7** body ≤110 行 / ≤3000 字符（先压缩再加 Soft 段）。

## Non-goals reaffirmed

- 不改 compose-next。
- Soft ≠ 第二编排层。
- 不删 R1/R2/R3。
